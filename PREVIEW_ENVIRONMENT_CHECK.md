# Preview environment check

This file exists only on the `preview-environment-check` branch to trigger and validate the isolated Vercel Preview environment. It contains no secrets and must not be merged into `main`.
